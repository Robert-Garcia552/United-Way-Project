<div>
    <div class="container-events ml-0 text-center">
      <h2><%= title  %> Events</h2>
      <% events.in_groups_of(3, Event.new) do |group| %>
          <% group.each do |event| %>
          <div class="d-flex flex-row flex-wrap m-0 justify-content-center">
              <div>
                <div >
                  <% if event.image.attached? %>
                      <%= image_tag rails_blob_url(event.image)  %>
                  <% else %>
                      <%= image_tag("home/UW-event.png", width: "400") %>
                  <% end %>
                </div>
                <div>
                  <b><%= link_to event.title, event %></b>
                </div>
              </div>
          <% end %> <!-- end event do -->
        </div>
      <% end %> <!-- end group do -->
  </div>
</div>

  <div>
  <%= link_to "All Events", events_path, class:"btn btn-default" %>
  </div>
